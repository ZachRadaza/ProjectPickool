# Changelog

## [0.1.3] - 2026-05-17

### Added
 - N/a

### Fixed
 - `Home.tsx`'s `Create +` button now correctly shows the `NoUserPopup.tsx` if no user is signed in.
 - Weird coloring gaps caused by scroll wheel.
 - Success message post account creation on `SignUpPopup.tsx` now shows accordingly.
 - `Join Club` button on `OpenedClubPopup.tsx` is now of the right width.
 - `NoUserPopup.tsx` on `User` page now shows up on load when the user does not have an account.
 - `EditUserPopup.tsx` width increases past screen when attaching a new image on desktop.
 - Scroll bar colors changed to match site colors.
 - Scroll Bar overflow on popups fixed.

### Changed
 - Uploaded images are not shrunk to help strorage space. Converted to webp aswell.